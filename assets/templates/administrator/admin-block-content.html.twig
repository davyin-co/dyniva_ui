{#
/**
 * @file
 * Default theme implementation for the content of an administrative block.
 *
 * Available variables:
 * - content: A list containing information about the block. Each element
 *   of the array represents an administrative menu item, and must at least
 *   contain the keys 'title', 'link_path', and 'localized_options', which are
 *   passed to l(). A 'description' key may also be provided.
 * - attributes: HTML attributes to be added to the element.
 * - compact: Boolean indicating whether compact mode is turned on or not.
 *
 * @see template_preprocess_admin_block_content()
 *
 * @ingroup themeable
 */
#}
{%
  set classes = [
    'list-group',
    compact ? 'compact',
  ]
%}
{% if content %}
  <table{{ attributes.addClass(classes) }}>
    <tbody>
      {% for item in content %}
        <tr class="list-group-item list-group-item-space">
          <td></td>
          {% if item.description %}<td></td>{% endif %}
        </tr>
        <tr class="list-group-item even">
          <td class="list-group__link">{{ item.link }}</td>
          {% if item.description %}
            <td class="list-group__description">{{ item.description }}</td>
          {% endif %}
        </tr>
      {% endfor %}
    </tbody>
  </table>
{% endif %}
